# Roadmap

路线图按“生产安全优先、质量指标可复现、失败结果可解释”的顺序排列。每项进入正式版本前，必须同时通过 Blender 3.3.5 与 5.2.0 LTS 回归，并更新安装包与 SHA-256。

## P0: 完成 0.5.7 布局验收（已完成）

- 重新构建安装包，使方向一致性、长矩形竖向优先和利用率排布进入 ZIP。
- 对拼接武器 Body、Gun Head、Bullet 重跑方向、分区、邻近、rack、覆盖率、winding、重叠和保存后重开审计。
- 明确记录“通过 / 安全拒绝并回滚 / 未覆盖”的对象级结果，禁止只报告 operator `FINISHED`。

0.5.7 已完成双版本回归、严格 smoke、ZIP 导入链和 SHA-256 校验；后续工作转入复杂 Unique 资产成功率与发布自动化。

## P1: 提升复杂 Unique 资产成功率

- 优先处理当前最常见的失败项：`relation_proximity_pass_rate`、`cardinal_island_rate`、`tile_polygon_coverage` 和 `aabb_fill`。
- 将结构关系、覆盖率和方向质量拆成可解释的分项评分，并在失败报告中给出触发门禁的岛、关系和候选轴。
- 增加长条、薄板、bevel 密集件、镜像件和近共线 N-gon 的固定夹具，形成最小可复现样本集。

## P1: 完善发布工程

- 增加确定性打包脚本，固定文件白名单、排序、时间戳和校验输出，避免手工重建造成 hash 漂移。
- 在 CI 中执行 Python `py_compile`、Blender 两版本回归、ZIP 解压导入和 SHA-256 校验。
- 将实验探针与发布物彻底分离，发布仓库只保留源码、文档、安装包和必要的轻量验证 JSON。

## P2: 工作流与可观察性

- 提供批量对象队列和 dry-run 报告，但每个对象仍保持独立事务，单个失败不得污染其他对象。
- 增加 JSON/HTML 诊断报告：源/目标 face-set、方向轴、残差、覆盖率、关系失败原因和回滚摘要。
- 增加动画姿态采样模式，在代表姿态副本上评估可见性；不直接修改绑定源网格。

## P2: 兼容性与性能

- 对大规模资产建立时间、内存和射线数量基线，限制最坏情况并提供可取消进度。
- 继续保持 Blender 3.3 API 下限，新增 API 必须提供兼容分支或明确降级说明。
- 评估 UDIM、Lightmap UV 和多对象材质工作流的显式保护策略，默认不改写非 Unique 合同。

## Release gates

正式版本必须满足：

1. 源码与 ZIP 文件逐文件 hash 一致，且 ZIP 无缓存、无重复 entry。
2. 两个 Blender 版本的三板斧通过：`py_compile`、相关回归、导入链验证。
3. Unique 结果满足 0-1、finite、正 winding、零退化、零正面积重叠和 face-set 分区不变。
4. 失败对象可复现地安全回滚，并在报告中说明原因；不能以“未崩溃”代替质量验收。
