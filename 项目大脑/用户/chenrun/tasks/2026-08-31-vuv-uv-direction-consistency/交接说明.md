# 49785 UV 兼容任务交接说明

最后更新：2026-09-04

当前状态：`in_progress`，尚无通过 Unique 门禁的 49785 输出

## 1. 接手先看

- 仓库：`C:\Users\SEASUN\Documents\ChatGPT\绑骨\sandbox\gitlab_uv_publish`
- 分支：`uv展开`
- 上一个远端基线：`fed877f fix: rebuild invalid refine sources`，插件版本 `0.5.9`
- 本次任务分支交付：插件 `0.5.10`，包含源码、测试、文档、哈希和 ZIP
- 当前工作项：`任务范围.md` 中 `asset-49785-compat`
- 项目总览：[Visibility Aware UV Optimizer 项目交接](../../../../../docs/Visibility_Aware_UV_Optimizer_项目交接.md)

接手时先运行 `git status --short --branch` 并阅读从 `fed877f` 到任务分支最新提交的完整 diff；如果工作区另有改动，不要擅自清理或覆盖。

## 2. 测试资产

源模型：

```text
C:\Users\SEASUN\Desktop\SK_CommonWeapon_Rig_1_49785.blend
```

源文件 SHA-256：

```text
47a520fb98fc8dd974f6326309bf1f5e479e8b2ae20556602ba7acd3abdd29fd
```

目标对象：

```text
SM_CDO_L1A_ChargeCannon_L_1_LOD1
```

测试脚本：

```text
C:\Users\SEASUN\Documents\ChatGPT\绑骨\sandbox\run_49785_vuv_test.py
```

源 `.blend` 必须保持只读语义。测试脚本把结果保存到独立输出目录，并校验源文件、几何、拓扑、源 UV 和 Seam 是否未变；禁止把输出路径设为源文件。

## 3. 版本状态

已提交的 `0.5.9` 能在 `Refine Layout` 源 UV 有正面积 overlap 时自动切换到 Hard Surface 重建，不再停在：

```text
Refine Layout source UV contains positive-area overlap
```

`0.5.10` 修复了硬表面 bounded repair 超预算后误入通用 Smart Project 回退、访问未初始化 `repair_faces` 的问题。该补丁的 Blender 3.3.5/5.2.0 定向回归和 strict smoke 已通过，但不能据此宣布 49785 成功，因为真实运行仍失败于 17 面图表。

## 4. 已完成实验

历史错误已依次越过：

```text
Unique UV Smart repair fragmented 32 charts into 382 charts (budget 256)
Unique UV Smart repair fragmented 51 charts into 543 charts (budget 408)
Refine Layout source UV contains positive-area overlap
```

处理原则：

- 不直接放宽碎片预算；
- Hard Surface 的过预算候选继续接受最终 Unique 门禁，而不是错误进入依赖通用修复局部变量的分支；
- 无效 Refine Layout Unique 源切换到 Hard Surface 重建；
- 所有失败必须回滚 UV 与 Seam 状态。

UV 参考边对比：

| 数据 | 岛数 | UV 不连续边 | 质量 |
| --- | ---: | ---: | --- |
| `UVMap2` | 261 | 5035 | overlap；148 degenerate；286 negative |
| `UVMap3` | 284 | 4756 | overlap；4833 degenerate；19 negative |
| UNION | 329 | 5552 | 仅作为软参考边证据 |
| INTERSECTION | 222 | 4239 | 仅作为软参考边证据 |

插件已支持：

```text
reference_uv_layers = UVMap2,UVMap3
reference_uv_boundary_mode = UNION | INTERSECTION
reference_uv_boundary_bias = 0.35
```

参考边用于种子和分割偏置，不锁定为 Seam，也不覆盖几何、材质和 Artist Seam 约束。

## 5. 当前真实阻断

最近一次 `0.5.10` 默认运行和 `0.5.9` Refine 自动重建运行都收敛到：

```text
Unique UV bounded repair could not repair chart with 17 faces
```

参考报告：

```text
C:\Users\SEASUN\Documents\ChatGPT\绑骨\sandbox\test_49785\result_refine_autorebuild\run_report.json
C:\Users\SEASUN\Documents\ChatGPT\绑骨\sandbox\test_49785\result_0510_default\run_report.json
```

前者记录 `0.5.9 + REFINE_LAYOUT + UVMap2/UVMap3 UNION`，后者记录本地 `0.5.10 + AUTO`。两者均为 `optimizer_failed`、`overall_valid=false`。

失败报告中的输出 `.blend` 只是回滚和诊断产物。`VUV_Test_058` 是测试脚本固定创建的目标 UV 层名，不代表版本号或成功状态；不得将这些 `.blend` 当作已生成的交付 UV。

## 6. 精确复现

在 PowerShell 中运行：

```powershell
& 'C:\Program Files\Blender Foundation\Blender 5.2\blender.exe' `
  --background 'C:\Users\SEASUN\Desktop\SK_CommonWeapon_Rig_1_49785.blend' `
  --python 'C:\Users\SEASUN\Documents\ChatGPT\绑骨\sandbox\run_49785_vuv_test.py' -- `
  --addon-root 'C:\Users\SEASUN\Documents\ChatGPT\绑骨\sandbox\gitlab_uv_publish\addons' `
  --output-dir 'C:\Users\SEASUN\Documents\ChatGPT\绑骨\sandbox\test_49785\result_refine_autorebuild' `
  --target 'SM_CDO_L1A_ChargeCannon_L_1_LOD1' `
  --source-layer 'UVMap2' `
  --initial-mode 'REFINE_LAYOUT' `
  --uv-usage 'UNIQUE' `
  --reference-uv-layers 'UVMap2' 'UVMap3' `
  --reference-uv-mode 'union'
```

为保留旧证据，接手人复现时应改用新的 `--output-dir`，不要覆盖上述两个参考报告。

## 7. 下一步排查顺序

1. 在 `_bounded_problem_chart_repairs` 中为失败图表记录 face indices、邻接、边界约束和每个候选失败原因，先确定 17 面图表是拓扑不可展开、候选质量失败还是数值问题。
2. 只对该问题图表比较 tree-cut unwrap、受限局部 unwrap、单图表 Smart Project 和安全投影候选；保持其他有效图表不变。
3. 验证候选的连通 disk、正 winding、退化、正面积 overlap、拉伸、0-1 和方向合同，禁止通过全局重切或提高预算掩盖失败。
4. 每次修改先跑 `tests/test_unique_uv_repair_fallback.py` 的 Blender 5.2 回归，再跑 49785；稳定后补 Blender 3.3.5。
5. 只有真实报告成功后，再重建 ZIP、更新哈希并执行双版本完整发布矩阵。

关键函数：

```text
addons/visibility_uv_optimizer/uv_optimize.py
  optimize_active_object
  _bounded_problem_chart_repairs
  _repair_remaining_problem_charts
  _audit_unique_layout
```

## 8. 验收标准

49785 必须同时满足：

- Blender 5.2 真实 Operator 返回成功；
- `run_report.json` 为 `status=success`、`overall_valid=true`；
- UV 坐标 finite、全部在 `0-1`；
- 0 degenerate、0 negative winding、0 positive-area overlap；
- 源 UV、几何、拓扑、Seam 和源 `.blend` 哈希不变；
- 输出保存后重开审计仍通过；
- `tests/test_unique_uv_repair_fallback.py` 在 Blender 3.3.5 和 5.2.0 都输出 `VUV_UNIQUE_LOCAL_REPAIR_OK`；
- 完整方向、结构布局和 strict smoke 发布矩阵通过；
- ZIP 解压后的真实导入链通过，源码、ZIP 和 SHA-256 一致。

## 9. 本次提交范围

`0.5.10` 和交接文档提交包括：

```text
CHANGELOG.md
README.md
addons/visibility_uv_optimizer/__init__.py
addons/visibility_uv_optimizer/uv_optimize.py
release/SHA256SUMS.txt
tests/test_unique_uv_repair_fallback.py
docs/Visibility_Aware_UV_Optimizer_项目交接.md
release/Visibility_Aware_UV_Optimizer_0.5.10_HardSurface.zip
release/Visibility_Aware_UV_Optimizer_0.5.10_HardSurface_ReferenceSplit.zip
项目大脑/用户/chenrun/代码地图/visibility-aware-uv-optimizer.md
项目大脑/用户/chenrun/tasks/2026-08-31-vuv-uv-direction-consistency/process.md
项目大脑/用户/chenrun/tasks/2026-08-31-vuv-uv-direction-consistency/任务指标.md
项目大脑/用户/chenrun/tasks/2026-08-31-vuv-uv-direction-consistency/任务范围.md
项目大脑/用户/chenrun/tasks/2026-08-31-vuv-uv-direction-consistency/交接说明.md
```

以上文件属于同一交付范围，不含 `.blend`、运行报告、临时目录或缓存。后续改动不可混入无关重构；再次提交或推送仍需用户明确要求。
