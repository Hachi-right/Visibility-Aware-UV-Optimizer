# 按 WPS IM 群初始化项目大脑用户 · 设计决策

开始日期: 2026-08-06

本文件只记录本任务形成的稳定设计决策。过程、尝试、失败路径和验收流水写入 `process.md`。

## 决策记录

- canonical author 取已与 WPS IM `user_id` 对齐的企业邮箱本地部分，不按姓名自行猜拼音。
- `likai1` 的 Leader 身份来自用户明确确认；群主身份只作为交叉证据，不自动推导其他组织权限。
- 其他群成员只登记为“成员（待确认）”，具体职责保持“待 Leader 分配”，避免把群成员关系误当成正式分工任命。
- 当前仓库尚无首个 commit，无法创建 worktree；首次模板引导在 `master` 完成，并由本任务四件套留痕。
