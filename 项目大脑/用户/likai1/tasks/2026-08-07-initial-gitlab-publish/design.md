# GitLab 首次发布 · 设计决策

开始日期: 2026-08-07

本文件只记录本任务形成的稳定设计决策。过程、尝试、失败路径和验收流水写入 `process.md`。

## 决策记录

- 用户给出的 URL 是 GitLab 群组地址；在该群组下创建 `MB-AIGC` 项目。
- 新项目不初始化 README、License 或 `.gitignore`，避免形成与本地无关的第二套 Git 历史。
- 项目可见性默认选择 Private；若群组策略强制其他级别，则以群组策略为准并记录。
- 本地首次提交目标分支统一为 `main`；禁止 force push。
