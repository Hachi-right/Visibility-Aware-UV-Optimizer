# 账号别名

> 用途: 将 Git 远端账号、历史误写账号或重复初始化账号映射到唯一 canonical 用户区。
> 规则: 冷启动、用户区初始化、收件投递、任务落盘和 workstate 识别时，先查本表再读写 `项目大脑/用户/{author}/`。
> 关联: `初始分工表.md` 记录成员职责；本表只解决 author → 用户目录映射。

## 别名表

| 别名 author | canonical 用户区 | 成员 | 说明 |
| --- | --- | --- | --- |
| （待补充） | — | — | 模板阶段无别名 |

## 使用规则

- 发收件、写任务、更新 tasklist 时，路径必须落到 canonical 用户区。
- 新增别名时同步更新本表、`author_aliases.py`，并在 canonical 用户区写清来源。
